(function (root) {
    "use strict";

    var constants = {
        // media player sizes
        playerSize: {
            tiny:"tiny",
            small: "small",
            large: "large"
        }
    };

    root.App.constants = constants;
    
})(window);