(function (root) {
    "use strict";

    root._V_.options.flash.swf = "components/video-js/video-js.swf";

    var app = app || {};
    app.utility = app.utility || {};
    app.models = app.models || {};
    app.controllers = app.controllers || {};

    root.App = app;    

})(window);